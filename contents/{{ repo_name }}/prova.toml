# Self-test manifest: resolve this plugin from its own directory (via prova-plugin.toml) and run
# tests/. A consumer would instead declare `{{ name }} = "prova-rs/prova-{{ name }}@v1"`.
[run]
paths = ["tests"]

[plugins]
{{ name }} = { path = "." }
